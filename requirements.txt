# 接口自动化框架运行依赖（版本下限，实际以本地为准）
pytest>=8.0
requests>=2.31
PyYAML>=6.0
jsonschema>=4.20
tenacity>=8.2

# 报告与质量门禁（可选，安装后 pytest 自动启用）
allure-pytest>=2.13
pytest-html>=4.1

# 开发期静态检查
# 版本钉死到 exact：静态检查也是门禁的一部分，本地和 CI 必须用同一个版本，
# 否则会出现"本地绿、CI 红"（曾经真的发生过：本地 0.15.21 / CI 0.16.8 默认规则不同）
ruff==0.16.8
